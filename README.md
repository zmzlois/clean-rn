# clean-rn

A simple CLI tool to clean all React Native caches in your project.

Usage:

```shell
npx clean-rn
```

Only clean ios:

```shell
npx clean-rn ios
```

Only clean android:

```shell
npx clean-rn android
```
